import styled from "styled-components";

export const FormHeader = styled.h2`
    font-size: 1em;
    padding-bottom: 1em;
`;

export const FormHeaderNoSpacing = styled(FormHeader)`
    padding: 0;
    margin: 0;
`;

export const FormHeaderMarginTop = styled(FormHeader)`
    margin-top: 2em;
`;